interface Props { }

export default function Page(props: Props) {
    return (
        <div className="w-full min-h-screen flex flex-col gap-4 p-4 text-black">
            machine-data-table
        </div>
    );
}